// Stub file that exists only so ESLint's `tests/**/*.ts` glob has at least
// one match before real test files are added. Can be removed once
// tests/bootstrap/poison-pill.test.ts (Task 5) lands.
export {};

